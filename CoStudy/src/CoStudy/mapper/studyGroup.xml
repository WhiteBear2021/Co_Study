<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CoStudy.mapper.StudyGroupMapper">
	<insert id="insertStudyGroup" parameterType="StudyGroup">
		insert into studygroup values(seq_studygroup.nextval,#{studygroup_name},#{studygroup_info},#{studygroup_location},0,10,1,sysdate,'N',#{isface},1,#{user_no})
		
	</insert>
	
	<select id="studyGroupList" resultType="StudyGroup">
		select * from studygroup
	</select>
</mapper>