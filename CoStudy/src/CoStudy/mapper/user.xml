<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CoStudy.mapper.UserMapper">
<cache />
  <select id="userInfo" resultType="User">
    select * from User_Info
  </select>
  
  <insert id = "insertUser" parameterType="User">
 	insert into user values(
 		USER_INFO_SEQ.NEXTVAL, #{user_no}, #{category_no}, #{user_id}, #{user_pw}, #{user_lastName}, #{user_firstName}, #{user_phoneNo}, #{user_post}, #{user_address1}, #{user_address2}, #{user_birth}, #{user_email}, SYSDATE
 	) 
  	
  </insert>
</mapper>